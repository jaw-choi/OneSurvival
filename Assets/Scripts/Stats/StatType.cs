public enum StatType
{
    MoveSpeed,
    GlobalDamage,
    CooldownReduction,   // cooldown *= (1 - value) if value is percent
    GlobalFireRate,      // fire rate multiplier
    ProjectileSpeed,
    PierceAdd,
    MaxHP,
    Regen,
    PickupRange
}
